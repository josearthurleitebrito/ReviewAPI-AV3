# Railway configuration — Backend service
# Worker and Frontend are configured via Railway UI (separate services)

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
