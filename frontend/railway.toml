# Railway configuration — Frontend service
# No Railway UI, defina o campo "Root Directory" como "frontend"

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
